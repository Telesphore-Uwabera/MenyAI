# Optional: backend API URL for production build.
# Leave unset to use runtime config (Settings page or login Backend URL).
# VITE_API_URL=https://menyai-nslw.onrender.com

# When building to be served by backend at /admin/, set base path:
# VITE_BASE_PATH=/admin/
