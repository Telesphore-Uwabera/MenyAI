# Admin panel environment (copy to .env for local; on Netlify set in Site settings → Environment variables)
# All VITE_* variables are baked in at build time.

# Backend API URL – used when no URL is set in the app (Settings / login).
# For Netlify: set this in Netlify Dashboard → Site → Environment variables.
VITE_API_URL=https://your-backend.onrender.com

# Only if building to be served under a subpath (e.g. backend at /admin/):
# VITE_BASE_PATH=/admin/
