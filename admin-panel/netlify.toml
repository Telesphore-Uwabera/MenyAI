# Netlify deploy config for MenyAI Admin Panel
# Connect this repo (or admin-panel as base directory) and set env vars in Netlify Dashboard.

[build]
  command = "pnpm build"
  publish = "dist"

# SPA: all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
