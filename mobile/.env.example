# MenyAI Mobile – copy to .env for production API URL
# Only needed when pointing at a deployed backend (e.g. Render).
# Do NOT commit .env if it contains secrets.

# Backend API URL (used when building the app)
# Leave unset for local dev: app uses http://localhost:4000 or http://10.0.2.2:4000
EXPO_PUBLIC_API_URL=https://menyai-nslw.onrender.com
